{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev:gateway --prefix src/gateway\" \"npm run dev:chat --prefix src/services/chat\" \"npm run dev:agent --prefix src/services/agent\" \"npm run dev:auth --prefix src/services/auth\" \"npm run dev:billing --prefix src/services/billing\"",
    "docker:redis":"docker compose up -d redis"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "concurrently": "^10.0.5",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-http-proxy": "^2.1.2",
    "ioredis": "^6.0.0"
  }
}
